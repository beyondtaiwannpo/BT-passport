# beyondtaiwannpo.com — 平台建置交接規格

> 整份貼進 Claude Code。第 7 節的答案已經填好了，不用再問一輪。

---

## 0. 任務

我是 Beyond Taiwan（BT）的幹部。BT 是台灣的學生非營利組織，幫公立高中生看見海外升學的選項。幹部團隊約 30 人，散在台灣、美國、加拿大、歐洲、澳洲、日韓。

我要建立 `beyondtaiwannpo.com` 這個網站。現在上面會有兩個功能（BT Passport、每週時間看板），**但這是一個會長大的平台**：之後會加更多功能，會由不同的人接手維護，而且會開放給學員（高中生）使用，不只幹部。

所以這次的重點**不只是把兩個工具放上去，而是把地基打對**。

先整份讀完，然後給我實作計畫，**不要直接開始寫程式**。

---

## 1. 三個現有資產

### 1-1. BT Passport（已上線，主體）

幹部的「數位護照」。33 格活動，蓋章、翻面、入境章、進度牆、匯出備份。

- Repo：**`beyondtaiwannpo/bt-site`**（2026-08-31 由 `BT-passport` 改名），GitHub Pages 從 `main` 根目錄發布
- 網域：**`beyondtaiwannpo.com`**（2026-08-31 從 `passport.beyondtaiwannpo.com` 搬過來，護照現在在 `/passport/`）
- 前端：**單一 HTML + 原生 JS，零建置步驟**
- 後端：Supabase，`sb_publishable_` 開頭的新版金鑰
- 網域註冊在 GoDaddy

**登入機制（這次會改，見第 3 節）：**

現在是 Supabase Auth 的 email + 密碼，註冊時填**邀請碼**，由 `auth.users` 的 trigger 驗證並扣減。「沒有邀請碼就沒有帳號」是資料庫層保證的，前端繞不過去。

**這次要加 Google 登入，並把邀請碼那道門往後移。** 詳細見 3-4、3-5。

> **不要加 LINE 登入。** Supabase 沒有內建 LINE，2024 年的功能請求到現在還沒做。
> LINE Login 是 OIDC 相容的，理論上可以走自訂 OIDC 供應商，但那要自己接、自己維護，
> 跟「單一 HTML、零建置、高中生維護」直接衝突。台灣高中生幾乎都有 Google 帳號，
> 覆蓋率不是問題。要做 LINE 等有人真的因為沒有 Google 帳號進不來再說。

> **不要改成魔術連結。**

### 1-2. BT-Scheduler（已上線，完全不動）

一次性的跨時區約時間工具，類似 When2meet。

`https://beyondtaiwannpo.github.io/BT-Scheduler/`

**不需要登入，這是刻意的**：要約海外校友、講者、合作單位（例如 AIT）時，不可能要求對方註冊帳號。

> **不要動它、不要合併、不要讓它需要登入。** 新首頁只放一張卡片連過去。

### 1-3. 每週時間看板（新做）

見第 4 節。

---

## 2. 目標架構

### 2-1. 一個網域、一個 repo、資料夾分頁面

```
beyondtaiwannpo.com/
├── index.html              對外的 BT 簡介，右上角一顆登入按鈕。不需要登入就看得到
├── CNAME                   內容改成 beyondtaiwannpo.com
├── shared/
│   ├── supabase.js         連線設定與 client（整站只有這一個）
│   ├── auth.js             登入、目前使用者、角色判斷
│   ├── brand.css           色票、字體
│   └── logo.png            旁邊放著，因為 CSS 檔載不了 PNG
├── vendor/                 supabase-js，第三方相依，平台層級
├── app/                    登入頁 + 登入後依角色顯示的功能選單
├── passport/               現有的 BT Passport 搬進來
└── availability/           新的每週時間看板
```

**`shared/` 是三個檔案加一張圖（2026-08-31，階段 2）。** 規格原本寫「只有三個檔案」，
`logo.png` 是第四個 —— 但它不是第四個「模組」，是 `brand.css` 那一行「色票、字體、logo」
裡的 logo 本身。CSS 檔沒辦法把 PNG 包進去，而護照與首頁需要的是**同一張圖**；
放兩份的話兩份遲早會不一樣。用 CSS `background-image` 就能少一個檔案，但那樣
`<img>` 的 alt 文字會消失，讀螢幕的人聽不到「Beyond Taiwan」——不值得。

`vendor/` 從 `passport/` 移到根目錄，因為 `shared/supabase.js` 要 import 它。
留在 `passport/` 的話會變成「共用層相依於某一個功能」，方向是反的。

**根目錄是對外的門面，不是登入框（2026-08-31 的決定）。** 原本的規劃是 `/` 直接放登入頁，改掉了。理由：以後會開放給學員，也會有人從搜尋或 IG 找過來，打開官網看到一個登入框很奇怪，而且那樣 BT 等於沒有對外的門面。有這一頁，`beyondtaiwannpo.com` 才是可以放進 IG 簡介、放進贊助提案的網址。

**`/app/` 一個頁面管兩種狀態：** 沒登入顯示登入表單（Google + email 兩條路），登入後顯示依 `role` 的功能卡片。根目錄那顆登入按鈕就是連到這裡。這樣「誰登入了、他是什麼角色」只有一個地方知道，`/` 完全不碰 auth。

用 `app/` 而不是 `login/`：這個資料夾在登入之後才是它主要的樣子（功能選單），叫 `login` 會讓人以為登入完就該離開它。而且資料夾分頁面是這份規格的基本形狀（見 2-3），多一個資料夾的成本就是多一個資料夾。

**為什麼是路徑不是子網域：** 瀏覽器的登入狀態綁定「來源」。`passport.beyondtaiwannpo.com` 和 `availability.beyondtaiwannpo.com` 在瀏覽器眼中是兩個不同的地方，localStorage 不共用，會被要求登入兩次。要跨子網域共用得改寫 Supabase 的憑證儲存方式、切成多個 cookie 再組回來，那段程式碼平常沒事、某天會突然壞掉，對每年換屆的學生團隊是負擔。

**同一個來源底下用資料夾，登入狀態自動共用，不需要寫任何額外程式。**

### 2-2. `/app/` 依角色顯示不同內容

登入後的 `/app/`，`cadre` 和 `student` 看到的卡片不一樣。（2026-08-31：這一節講的是 `/app/`，不是根目錄。根目錄是對外簡介，見 2-1。）

| 角色 | 看得到 |
|---|---|
| `cadre` | BT Passport、我的時間、約一場會議 |
| `student` | 目前沒有功能。顯示一段說明 + 一個「我是 BT 幹部，我有邀請碼」的入口 |

`student` 現在是空的沒關係，那個角色存在的意義是**結構先做對**。之後給學員的功能加進來時，不用回頭改權限。

**卡片的顯示不能只靠前端判斷。** 學員把卡片點出來也應該進不去——防線在每個功能自己的 RLS，前端只是不顯示而已。

### 2-3. `shared/` 只有三個檔案，這是刻意的

**不要做路由器、不要做外掛系統、不要做管理後台。**

理由：這個團隊每年換屆，維護者是高中生和大學生。護照能做成，是因為它一路都是可運作的東西，不是先蓋一個大架構再往裡面填。

加一個新功能的成本應該是：**開一個資料夾、放一個 HTML、讀那三個共用檔案。** 就這樣。

等真的有第三、第四個功能出現，再看要不要抽更多東西出來。**現在多蓋的每一層，都是明年某個人看不懂但不敢刪的東西。**

---

## 3. 資料層與登入：現在就要做對的事

這一節是整份規格最重要的部分。這些事現在做很便宜，**發碼給三十個人之後做會很貴**。

### 3-1. 角色 role

現在只有幹部。但**以後要開放給學員**，而學員不能看到幹部的作息表，也不該進護照的進度牆。

如果現在不做，之後每加一個功能都要重寫一次權限，而且那時候資料庫裡已經有三十個人的資料。

**做法：** 使用者資料加一個 `role` 欄位，值為 `cadre` / `student` / `alumni`。

**預設值分兩段，不是一個值（2026-08-31 補；原本寫「預設 `cadre`」，跟 3-5 那句「預設 `student`」互相矛盾）：**

| 時機 | default | 為什麼 |
|---|---|---|
| 階段 3+4（加欄位、拆表） | `'cadre'` | 這時候守門還是註冊時的邀請碼，能註冊的都是幹部。既有的兩列也就地是 `cadre` |
| 階段 5 的**最後一句** | `'student'` | 邀請碼的門移到升級之後，任何人都能登入了 |

那一句 `alter column set default` 要**單獨一個 commit、單獨確認**：它是整輪唯一一個守門變弱的瞬間。順序反了（Google 開了但 default 還是 `cadre`）等於任何人登入就是幹部，而且不會有任何東西報錯。

`set default` **只影響之後新建的列，不會回填**，所以既有的兩個帳號仍然是 `cadre`。

**`alumni` 這一版沒有任何自己的權限：** check constraint 允許三個值，但所有 RLS 只認 `cadre`，`alumni` 等同 `student`（什麼都看不到）。等真的有校友要進來再定義。

所有現有的 RLS 政策，凡是寫「登入者皆可讀」的，改成「`role = 'cadre'` 才可讀」。包含 `passports`、`stamps`、`activities`、`months`、`destinations`。

**寫入端也要認 `cadre`（2026-08-31 補，原本漏了）。** `stamps`、`entries`、`visas` 現在的 insert / update / delete 政策只綁 `auth.uid() = user_id`，也就是**任何登入者都寫得進去**，只有讀取端有角色過濾。一層不夠：學員雖然讀不到別人的 `entries`，卻能往自己名下塞資料、把自己蓋成一本護照。三張表的每一條寫入政策都要加上 `role = 'cadre'`，跟讀取端同一個條件。

**現在只有一種值，但結構要在。**

#### 這裡有一個洞，一定要處理

角色決定看得到什麼。而每個人都需要能改自己的資料（名字、時區）。

**如果 `profiles` 整列可以自己更新，任何學員都能把自己的 `role` 改成 `cadre`**，然後看到三十個人的作息表。RLS 擋不住這個，因為那是同一列的合法更新。

解法是**欄位層級的權限**：

```sql
grant update (name_zh, name_en, team, tz, avatar) on profiles to authenticated;
```

只發這幾個欄位，`role` 完全不在裡面。要改角色只能透過一個 `security definer` 的函式，而那個函式會驗邀請碼（見 3-5）。

> **2026-08-31 修正：原本這行漏了 `team`。** 3-2 的表格把 `team` 列在 `profiles` 裡，這一行卻沒有 —— 規格自己前後不一致，而遷移 A 照這行逐字抄。
>
> 後果比「team 存不進去」嚴重得多：**Postgres 要求 UPDATE 的 `SET` 清單裡每一欄都要有權限，缺一欄整句被拒。** `data.js` 有三處會把 `team` 跟名字寫在同一句，所以「護照資料頁存檔」「清除這本護照」「匯入還原」三條路整條都壞掉。
>
> 兩層驗收都如實地放它過去：遷移 A 的驗收比對的是「這行範例列了哪幾欄」而不是「前端實際會寫哪幾欄」；§8-3 的 API 實測那一句只寫 `name_zh`，剛好是有權限的那一欄。**量了，但量的是最容易過的那個案例。**
>
> 已經補上 `supabase/migrations/2026-08-31-profiles-grant-team.sql`，並在 `check.sh` 加了一條守門：前端寫進 `profiles` 的每一欄都必須在允許清單裡，而且寫入點的**數量**也要對得上（否則掃描範圍會安靜地縮小）。

**這跟 `visas` 不給 UPDATE 權限是同一個原則**：不能靠前端自律，要讓資料庫擋。

驗收方式：用一個 `role = 'student'` 的帳號，直接對 API 送 `update profiles set role='cadre'`，**要看到權限錯誤，不是靠前端沒有那個按鈕。**

### 3-2. 拆表：`profiles` 與 `passports`

現在 `passports` 這張表同時是「使用者帳號」和「護照資料」。

加了學員之後，一個沒有護照的學員也需要一列 `passports`,那很怪，而且下一個接手的人會誤解這張表在幹嘛。

**建議的拆法：**

| 表 | 放什麼 | 誰有 |
|---|---|---|
| `profiles` | `id`、`name_zh`、`name_en`、`team`、`role`、`tz`、`avatar`、`updated_at` | 每一個使用者 |
| `passports` | `id`、`motto`、`issued`、`intro_seen` | 只有幹部 |

`tz` 是 IANA 時區字串，看板要用（見第 5 節）。放在 `profiles` 因為那是「這個人」的屬性，不是「這本護照」的屬性。

**外鍵要跟著改指 `profiles`（2026-08-31 補，原本沒寫）。** `stamps`、`entries` 現在的 `user_id` 指向 `passports.id`，`visas` 指向 `auth.users`。拆表之後三張都改指 `profiles(id)` —— 每一個使用者都有 `profiles` 那一列，指過去才是對的關係。

這不是形式問題，會決定進度牆活不活得下來：進度牆是一句內嵌查 `from("passports").select("id, name_zh, ..., stamps(...)")`，靠 `stamps.user_id → passports.id` 這條外鍵推導。名字搬到 `profiles` 之後那句會斷，要改成 `from("profiles")`，而那需要外鍵指過去。**不要改成先查人再逐人查章 —— 三十個人就是三十一次往返。** 進度牆的過濾條件同時要加上 `role = 'cadre'`。

另外，`add constraint ... references profiles(id)` 這句本身就是一道免費的守門：搬遷不完整的話它會失敗、整份回滾。

**這是搬遷，有風險。所以順序很重要，見第 8 節。**

### 3-3. 註冊 trigger 要跟著改

現在的 `handle_new_user()` 驗證邀請碼然後 `insert into passports(id)`。拆表之後要改成建 `profiles`，`passports` 只有幹部才建。

**這段是整個系統唯一的守門，改的時候特別小心，改完必須重跑 `supabase/rls-test.sql`。**

**`intro_seen` 要特別驗（2026-08-31 補）。** 它在 `passports`，而**學員沒有 `passports` 那一列**。前端讀 `intro_seen` 的路徑必須能接受「這個人根本沒有護照」，不能因為查回 null 就炸掉、也不能因此把引導頁再跳一次。驗收要用一個真的 `student` 帳號登入看，不是用改過 `role` 的幹部帳號看。

### 3-4. 加 Google 登入

**Google 是 Supabase 內建的供應商**，在 Google Cloud 開一個 OAuth client、把 client ID 與 secret 填進 Supabase 後台就好。前端只多一顆按鈕呼叫 `signInWithOAuth`。

**保留 email + 密碼當備援。** 有人沒有 Google 帳號、有人在中國、有人 Google 登不進去。兩條路並存，不要拿掉舊的。

### 3-5. 邀請碼那道門要往後移（這是這次最關鍵的改動）

**問題：** 現在的守門靠「註冊表單填邀請碼 → 進 `raw_user_meta_data` → trigger 驗證」。

**Google 登入沒有這個時機。** 帳號在 OAuth 回來的那一瞬間就建好了，使用者根本還沒機會填任何東西。所以「註冊時選學員或成員」在 OAuth 底下不成立，登入的當下還沒有表單可以選。

**改法：把門從「帳號創建」移到「角色升級」。**

| 現在 | 改成 |
|---|---|
| 沒有邀請碼 → 建不了帳號 | 任何人都能登入，`profiles.role` 預設 `student` |
| | 想成為幹部 → 登入後在設定頁輸入邀請碼 → 驗證通過才升級成 `cadre` |

守門還在，只是換了觸發點。而且**這樣其實更合理**：以後學員本來就該能自己註冊，不需要碼。

**實作要求：**

1. 升級走一個 `security definer` 的 RPC 函式，裡面驗證邀請碼並扣減，跟現在的 trigger 是同一套邏輯
2. 那個函式要用 `update invite_codes set uses_left = uses_left - 1 where ... and uses_left > 0` 加 `if not found`，**一句做完**。分成先查再扣會讓兩個人同時用掉同一組最後一次的碼
3. `set search_path = public, pg_temp`，跟現有的 trigger 一樣
4. **前端不准有任何「設定角色」的路徑**，只能呼叫這個函式
5. 升級成 `cadre` 的時候才建 `passports` 那一列

**這一改會讓現有的 `rls-test.sql` 有一大段失效**（它測的是「無效邀請碼註冊失敗」）。要改寫成測新的升級路徑，並且做反向驗證：**拿掉邀請碼檢查，確認測試會 FAIL。**

### 3-6. 外接寄信

**只有 email + 密碼那條路需要寄信**（確認信與重設密碼）。Google 那條路的 email 已經被 Google 驗證過，不用寄。

既然 email + 密碼保留當備援，寄信就還是要做。

**四件事：**

1. **Resend 帳號與網域驗證。** 在 GoDaddy 加 SPF / DKIM 記錄，跟第 8-1 節的 A 記錄同一趟做完
2. **Supabase → Authentication → SMTP Settings** 填入設定。**程式碼一行都不用改**
3. **開啟 email 確認**，並確認既有帳號不受影響。現在只有一個帳號，先拿它測
4. **做一個重設密碼的頁面**，處理 recovery token

**寄件地址用組織信箱**，不要用任何個人信箱。**信件模板改成中文為主**，收信的是台灣高中生。

**README 要跟著改。** 現在有一整節寫「管理者代設密碼、不寄信」和八種帳號問題的處理流程，那一節會過期。按 README 第 11 項處理：改寫成新做法，並記下舊做法為什麼被取代，以及它當初為什麼成立（前提是「使用者只有 30 個幹部」）。

---

## 4. 每週時間看板

### 4-1. 要解決的問題

現在每次要開會，都得開一個新的 When2meet 連結、在群組請大家填、一個一個催。

改成：**每個人維護自己的「每週固定有空時間」**，要約會議時直接看看板挑時段，只需要在群組說一句「請大家更新一下自己的時間」。

### 4-2. 存的是「每週固定」，不是「特定日期」

這是整個設計的核心。存「8/24 晚上有空」一週後就過期，等於沒解決問題。

要存「**每週一 19:00 到 22:00 有空**」這種相對時間，更新頻率才會從「每場會議一次」降到「一學期一兩次」。

### 4-3. 三個區塊

**A. 團隊看板**

- 一週的格線：欄是星期一到日，列是時段，半小時一格
- 顏色深淺代表有空的人數
- 滑過或點一格顯示「誰有空、誰沒空」
- 可以往前後翻週（夏令時間會讓不同週的換算結果不同）
- 點任一格顯示這個時段在所有成員所在地的當地時間，可一鍵複製，並提供加到 Google 日曆的連結

**B. 我的每週時間**

- 同樣的週格線，塗自己有空的時段
- **一定要有批次填寫**：選「從 19:00 到 22:00」加上勾選要套用的星期（含「平日」「週末」「全部」快捷），按套用
  拖曳塗一百個格子在手機上不可行，這點已經實測過
- 也要能「複製星期一到其他天」
- 存檔後更新 `updated_at`

**C. 成員清單**

- 每個人顯示：名字、所在地、**上次更新：N 天前**
- 超過 30 天標紅

**這一區是讓看板活下去的關鍵**：沒有它就不知道該催誰，兩個月後大家就不信任看板了。

### 4-4. 這一版不做「當週例外」

「這週三我要考試，雖然平常有空但這次不行」這種功能**這一版不要做**。

理由：在確定大家真的會用之前先把系統做複雜，如果沒人用就白做了。過渡期口頭講一聲成本很低。

但**資料結構要預留擴充空間**，之後要加不用打掉重做。

### 4-5. 隱私：這比護照的心得更敏感

看板存的是三十個人的**長期作息表**：哪一天誰固定不在、誰每個週末都有空、誰週三晚上永遠沒空。

**幹部裡有未成年成員。** 這比一次性的投票敏感得多。

三個要求：

1. **只有 `role = 'cadre'` 且登入的人能讀。** 學員、未登入的人一律讀不到
2. **每個人只能改自己的。** RLS 的 `using` 和 `with check` 都要綁 `auth.uid()`
3. **第一次進入看板時要明講**：「你填的每週時間，其他 BT 幹部看得到。」護照的註冊頁已經有這個做法，沿用那個語氣

#### 這個功能的形狀就是把作息表發給所有幹部（2026-09-02 補）

看板要顯示「誰有空、誰沒空」，而時區換算必須在**觀看者**的瀏覽器裡做（§5-2），
所以**每個幹部的瀏覽器都會拿到全部三十個人的完整作息表**。不是只拿到人數統計，
是每個人每一格的原始資料。

這不是實作偷懶，是功能本身的形狀：沒有辦法「只給他看得到的部分」，因為看板就是
要給他看。RLS 能做的是把這件事限制在 `role = 'cadre'`，**不能讓它變得更少**。

這件事有兩個後果，都要接受：

- 護照的心得是「只有本人看得到」，看板不是。兩者的保護等級不同，
  而它們在同一個網站上、用同一個帳號登入 —— 使用者不會自動知道這個差別
- 一個幹部想把全部人的作息表存下來，他不需要任何技巧，打開瀏覽器的
  開發者工具就有了。這條路關不掉

所以上面第 3 點那句告知不是禮貌用語，**是這個功能唯一的知情同意**。
它必須在第一次進去、開始填之前出現，不能塞在設定頁或說明頁裡。

---

## 5. 時區處理（最容易做錯的部分）

BT 的幹部散在全球，這件事做錯整個工具就沒有價值。

### 5-1. 儲存

每個人的每週時段，以**他自己的 IANA 時區**為準儲存。

例如「台北的 Ann，每週一 19:00」存成 `{weekday: 1, minute: 1140}`，加上她 `profiles.tz` 的 `"Asia/Taipei"`。

**不要存成 UTC 偏移量**（例如 +8）。夏令時間會讓偏移量在一年中改變，存死了就會錯。

### 5-2. 顯示

顯示某一週時，對每個人的每個時段：

1. 找出那一週中該星期幾的實際日期（以該成員的時區為準）
2. 用該成員的時區把「日期加當地時間」換算成真實的 UTC 瞬間
3. 再把那個瞬間換算成**觀看者**的時區顯示

這樣夏令時間切換時，台北與美國的時差會自動從 12 小時變 13 小時，不需要任何特殊處理。

### 5-3. 實作方式

用瀏覽器內建的 `Intl.DateTimeFormat` 加 IANA 時區資料庫，**不要引入 moment 或 dayjs**。

```js
function partsIn(date, tz){
  const f = new Intl.DateTimeFormat("en-US",{timeZone:tz,hour12:false,
    year:"numeric",month:"2-digit",day:"2-digit",
    hour:"2-digit",minute:"2-digit",second:"2-digit"});
  const p = {};
  for (const x of f.formatToParts(date)) if (x.type!=="literal") p[x.type]=+x.value;
  if (p.hour === 24) p.hour = 0;
  return p;
}
function offsetOf(date, tz){
  const p = partsIn(date, tz);
  return Date.UTC(p.year,p.month-1,p.day,p.hour,p.minute,p.second) - date.getTime();
}
function toInstant(y,m,d,h,mi,tz){
  const guess = Date.UTC(y,m-1,d,h,mi);
  const o1 = offsetOf(new Date(guess), tz);
  let t = guess - o1;
  const o2 = offsetOf(new Date(t), tz);
  if (o2 !== o1) t = guess - o2;   // 跨越 DST 邊界時修正
  return new Date(t);
}
```

這段已經在 BT-Scheduler 上線驗證過，包含美國春季 DST 那天不存在的 02:00 到 02:59 會自動收合。**直接沿用，不要自己重寫。**

### 5-4. 邊界情況

**星期會錯位。** 台北的「週一 08:00」在美東是「週日 19:00」或「週日 20:00」。看板的欄位要以**觀看者**的星期為準，成員的時段對應過去。

**DST 那兩天。** 春季有一小時不存在，秋季有一小時重複。不用特殊處理，但要知道會發生，不要當成 bug。

**時區偵測。** 用 `Intl.DateTimeFormat().resolvedOptions().timeZone` 自動偵測，但**一定要讓使用者手動改**，而且要能打字搜尋（打「Michigan」「密西根」「安娜堡」都要找得到）。不要用純下拉選單，幹部通常不知道自己在哪個 IANA 時區。

瀏覽器只給得到 IANA 名稱（`America/Detroit`），中文與地名別名要自己維護一份常數表，在「不引進套件」之下這是唯一的做法。

**這份別名表至少要涵蓋 `destinations` 那 24 個城市（2026-08-31 補）。** 那份名單就是 BT 的人真的在的地方，而且 `NASHVILLE`、`MADISON`、`CHARLOTTE`、`SALT LAKE CITY`、`ROCHESTER` 這種小城市，使用者更不可能知道自己該選哪個 IANA 時區 —— 大城市他還猜得到，小城市完全猜不到。範圍就以那 24 個為底，加上台／美／加／歐／澳／日韓的常見地區，不做全世界。

---

## 6. 硬規則

### 技術

1. **單一 HTML + 原生 JS。不要 React、Vue、build step、npm。** 維護者是高中生和大學生，每年換屆
2. **必須跟 Passport 在同一個 Supabase 專案**，否則帳號不共用
3. **必須跟 Passport 同一個來源。** 同一個 repo、同一個網域、用資料夾區分
4. **寄信只透過 Supabase 的 SMTP 設定（Resend），不要在前端自己呼叫寄信 API。** 那會把金鑰放進公開的程式碼
5. **不要用 `sb_secret_` 或 service_role 金鑰。** 前端只能用 `sb_publishable_`
6. **RLS 必須設對，而且建表之後記得 `grant`。** 只設 policy 不 grant 會拿到 `42501 permission denied`，這是踩過的坑
7. GitHub Pages 免費方案只能從 **public** repo 發布。程式碼公開是安全的，防線在 RLS
8. **`role` 不可以由使用者自己更新。** 用欄位層級的 grant 擋，不是靠前端沒有那個按鈕（見 3-1）
9. **不要加 LINE 登入**，Supabase 沒有內建（見 1-1）

### 品牌

- **一個畫面最多 3 種顏色**
- **一個畫面最多 2 種字體**
- **一定要有 Beyond Taiwan logo**

| 用途 | Hex |
|---|---|
| 主橘（標題、重點、CTA） | `#FFC46C` |
| 米白（背景） | `#EDE5D8` |
| 中性灰（分隔、次要資訊） | `#C6C6C6` |
| 淺橘（柔和背景） | `#FFE8C5` |
| 深藍（點綴，可作標題色塊） | `#102A86` |

標題 **Barlow Condensed**，內文 **Inter**，中文搭系統 CJK fallback。**不要載入中文網頁字體**，那會變成第三種字體。

Logo 不可改色、拉伸、旋轉、調透明度。

介面文字**中文為主、英文為輔**（例如「我的時間 / My availability」）。

---

## 7. 你會問的問題，答案在這裡

**Supabase Project URL**
`https://norjaglyaotzewxavmhv.supabase.co`

**Passport 穩定了嗎**
穩定，已上線並實測過。`check.sh` 30 項、88 個測試全綠。

**幹部帳號怎麼開**
每個人自己註冊，但需要邀請碼。`invite_codes` 表，扣減寫在 `auth.users` 的 trigger 裡，前端繞不過去。

**現有的使用者資料表**
`passports`：`id`（= `auth.users.id`）、`name_zh`、`name_en`、`team`、`motto`、`avatar`、`issued`、`intro_seen`、`updated_at`。
**有名字，沒有時區，沒有角色。** 這兩個要加，見第 3 節。

其他既有的表：`activities`、`months`、`stamps`、`entries`、`visas`、`destinations`、`invite_codes`、`milestones`（已停用但保留）。

**登入方式怎麼處理**
這次要加 Google 登入（內建供應商），保留 email + 密碼當備援。**不要加 LINE**，Supabase 沒有內建。
邀請碼從「註冊時驗證」改成「登入後升級角色時驗證」，見 3-5。

**寄信怎麼處理**
接 Resend，見 3-6。只有 email + 密碼那條路需要寄信，Google 那條不用。

**學員什麼時候進來**
還沒有時間表。這次只把 `role` 的結構做對，`student` 的功能之後再說。

**`passport.beyondtaiwannpo.com` 發出去了嗎**
**還沒發給幹部**，只有我自己在用。所以搬完之後那筆 DNS 記錄可以直接刪掉，不用做轉址。
（2026-08-31：已照這個做，`passport` 那筆 CNAME 刪掉，沒有做任何轉址。）

**根網域上原本有東西嗎**
有，但不重要。2026-08-31 查到 `beyondtaiwannpo.com` 回 HTTP 200、標題 `Beyond Taiwan NPO`、`generator` 是 `Go Daddy Website Builder`。那是**買網域時自動產生的預設頁**，不是任何人做的、也沒有在付費，使用者的裁定是不要它。A 記錄一改它就消失，不需要退訂任何東西。它也沒有連到護照（查過，只連回自己首頁）。

記下來是因為：下一個看到「把根網域的 A 記錄改掉」這句話的人，值得知道當時**確認過**那上面沒有別人的東西，而不是沒查就改。

---

## 8. 執行順序

**這個順序不能改，而且第 1 到 6 步做完之前不發碼給幹部。**

| # | 做什麼 | 為什麼是這個順序 |
|---|---|---|
| 1 | 搬網域與資料夾，護照跑在 `/passport/` | 這步壞了後面都做不了 |
| 2 | 抽出 `shared/`，護照改用它 | 用已經在跑的功能驗證共用層，比先蓋空架構再往裡塞可靠 |
| 3 | 加 `role` 欄位，更新所有 RLS | 資料還少，最便宜 |
| 4 | 拆 `profiles` / `passports`，改 trigger | 同上，而且要趁只有一個人在用 |
| 5 | Google 登入、邀請碼改成角色升級、改寫 `rls-test.sql` | 這步動到唯一的守門，要在資料還少的時候做 |
| 6 | 外接寄信、email 確認、重設密碼頁 | DNS 跟第 1 步同一趟做完 |
| 7 | 對外簡介頁 `/` 與 `/app/`（登入 + 依角色顯示卡片） | 有東西可以連了才做。`/` 的文案由使用者提供，先放佔位字 |
| 8 | 每週時間看板 | 第一個用新架構蓋的功能，也是對架構的驗證 |

**第 3、4、5 步是這份規格的重點。** 現在資料庫裡只有一個使用者，搬表的成本接近零。三十個人開始蓋章之後再搬，要寫 migration、要停機、要備份，而 Supabase 免費方案**沒有任何備份**。

### 8-1. 網域與 DNS

**GoDaddy 要改：**

- 根網域 `beyondtaiwannpo.com` 需要**四筆 A 記錄**：`185.199.108.153`、`185.199.109.153`、`185.199.110.153`、`185.199.111.153`
  （根網域不能用 CNAME，這是 DNS 規範的限制）
- `www` 加一筆 CNAME 指向 `beyondtaiwannpo.github.io`
- **`passport` 那筆 CNAME 直接刪掉**（還沒發給任何人）
- **Resend 的 SPF / DKIM 記錄一起加**（見 3-6），同一趟進 GoDaddy 做完

**驗證順序：** 先確認 DNS 生效（`host beyondtaiwannpo.com` 看得到那四個 IP），**再**去 GitHub Settings → Pages 設自訂網域。順序反了會分不清是網站壞了還是 DNS 還沒生效。

> **⚠ repo 裡的 `CNAME` 檔就是自訂網域的設定，push 出去等於當場切換（2026-08-31 補）。**
>
> `CNAME` 的內容已經在本機改成 `beyondtaiwannpo.com` 了，但**在 `host beyondtaiwannpo.com`
> 看得到那四個 GitHub IP 之前不要 push**。太早 push 的話：GitHub Pages 會把自訂網域
> 切到一個 DNS 還沒指過來的位址，`passport.beyondtaiwannpo.com` 同時失效，
> 結果是兩個網址都打不開，而且憑證要重簽。
>
> 順序是：**GoDaddy 改 DNS → `host` 確認四個 IP → 才 push。**
> 這一句是給下一個人看的，不是給今天的你看的 —— 今天你知道，三個月後沒人記得
> 「那個檔案不是文件，是開關」。

設好後等 HTTPS 憑證簽發，可能要幾分鐘到一小時，這段期間出現憑證警告是正常的。

**Supabase 那邊也要改：** Authentication → URL Configuration 的 Site URL 與 Redirect URLs 更新成新網域。

**Repo 名稱**建議改成 `bt-site` 或類似的，但**先問我**，改名會影響本機的 git remote。

### 8-2. 搬遷的驗收條件

護照是單一 HTML，多數相對路徑不受影響，但要 grep 一次有沒有寫死 `/BT-passport/` 或 `passport.beyondtaiwannpo.com` 的絕對路徑。

**2026-08-31 grep 過了，結論是前端零改動：** `index.html`、`src/`、`site.webmanifest` 全部用 `./` 相對路徑，沒有任何絕對網址或絕對路徑。搬進 `/passport/` 之後 PWA 的 `scope` 會自動變成 `/passport/`，那是對的行為。

唯一出現 `bt-passport` 字樣的程式碼是 `src/main.js` 的匯出檔名 `bt-passport-<護照號碼>-<日期>.json` —— **那個不要改**。改的是 repo 名稱與網域，產品還是叫 BT Passport，而且改了會讓幹部手上的舊備份檔跟新檔名對不起來。

**grep 要用 `grep -r`，不要用 `git grep`。** 這份規格檔在 2026-08-31 當下還沒有被 git 追蹤，`git grep` 看不到它 —— 而它正是最需要被一起更新的那一份。

**驗收不是「護照能打開」，是資料完整：**

- 已經蓋的章全部都在
- 已經拿到的入境章城市**沒有改變**（`visas` 表存的是不可撤銷的紀錄）
- 心得和照片都在
- 匯出備份仍然可以還原
- `intro_seen` 沒有被重設（不該又跳一次引導頁）

**「畫面能顯示」不等於「資料還在」。** 這五項要逐項確認。

### 8-3. 角色與權限的驗收

第 3、5 步做完之後，**用兩個帳號實測，不看畫面看資料庫回傳：**

1. 開一個 `role = 'student'` 的帳號
2. 用它對 API 直接查 `profiles`、`stamps`、看板的資料表 → **應該回 0 列或權限錯誤**
3. 用它送 `update profiles set role='cadre'` → **應該被權限擋下**
4. 用一組有效的邀請碼呼叫升級函式 → 成功變成 `cadre`
5. 再查一次上面那些表 → 這次應該查得到

**第 2、3 項是重點，而且要用 API 直接打，不是靠前端沒有那個按鈕。** 這是護照那邊驗 `entries` 隔離時用過的做法。

---

## 9. 工作方式

- **分階段做，每個階段做完停下來讓我確認。** 不要一路衝到底
- 第 1 步（搬遷）做完一定要停下來讓我實測，帶著一個壞掉的護照往下做新功能會很難除錯
- 第 3、4 步（改資料庫）**SQL 一律先貼給我看過，我自己去 Supabase 執行**，不要你自己跑
- 每個階段結束時告訴我：改了哪些檔案、我要自己去點哪裡驗證、預期看到什麼
- 不要重構或「順手改善」既有程式碼。發現真正的 bug 就告訴我，由我決定要不要改
- 破壞性操作（刪檔案、`git push --force`、改資料表結構）一律先問
- **中文回覆我**

### 9-1. 驗收要包含反向確認

這個 repo 已經有一份 README 記著一條原則，這次也適用：

> **「我驗過了」和「我驗的是該驗的東西」是兩件事。**

每個階段的驗收，除了「功能會動」，還要問一次「**壞掉的時候會不會被發現**」。

具體做法：任何新的守門或測試，都要**故意弄壞一次確認它會 FAIL**。這個 repo 反覆踩到「檢查通過但檢查的不是該檢查的東西」——同義反覆的斷言、`grep -c` 數行不數次數、取樣太小看不出偏斜、測試在錯誤的工作樹上跑、移除字串讓負向斷言安靜地死掉、破壞落在別的地方而不是被測的那一段。**完整的清單在 README 第 12 項**，那裡是唯一的真相來源；這裡不重複列舉，也不寫「第幾次」——那個數字每加一個例子就會過期。

判準也在那裡：**一條測試如果在它被測的東西壞掉時仍然通過，它的名字就是錯的**——不是測試不夠嚴，是它測的根本不是那件事。那兩種診斷指向完全不同的修法。

現有的 README 第 11、12 項寫著這些教訓，**請先讀過再開始**。

---

## 10. 不要做的事

- 不要動 `BT-Scheduler`，也不要讓它需要登入
- 不要改 Passport 的登入方式 —— 指的是**不要換成魔術連結、不要加 LINE、不要拿掉 email + 密碼**。
  加 Google 登入、把邀請碼從註冊移到角色升級，是這一輪明確要做的（見 3-4、3-5），不在這條的範圍裡。
  （2026-08-31 改寫：原文只寫「不要改 Passport 的登入方式」，跟 3-4、3-5 直接衝突。）
- 不要引進前端框架
- **不要加 LINE 登入**（Supabase 沒有內建，要自己接 OIDC）
- 不要讓 `role` 變成使用者可以自己更新的欄位
- 不要做「當週例外」功能（這一版）
- 不要自己重寫時區換算，用 5-3 那段
- **不要為了「以後可能會加功能」先蓋路由器、外掛系統或管理後台**
- 不要在第 1 到 6 步完成之前催我發碼
