# 活動格子翻面 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 活動格子變成可翻面的卡片：正面是未蓋章的資訊與蓋章入口，背面是章、日期、心得、照片。

**Architecture:** `.slot` 從 `<button>` 改成 `<div>`（按鈕不能巢狀），內含一層 `.flip` 用 CSS Grid 把兩面疊在同一格、`transform-style:preserve-3d` 做翻轉。哪一面朝上由 `ui.js` 的純函式 `faceOf(S, a)` 單一決定，狀態存在 `S.flipped`。翻面與落章都是插入時播放的 keyframe animation（`render()` 是 `innerHTML` 全量重繪，transition 不會跑），兩段用 `animation-delay` 串接。

**Tech Stack:** 原生 ES module、CSS 3D transform、`node --test`（node 24 內建）、bash（`check.sh`）。無套件管理器、無 build step。

**Spec:** `docs/superpowers/specs/2026-08-25-slot-flip-design.md`

## Global Constraints

- **最多 3 種顏色**：`#FFC46C`、`#EDE5D8`、`#102A86`。深淺只能調透明度，且只允許 `rgba(16,42,134,α)`、`rgba(255,196,108,α)`、`rgba(255,255,255,α)`。不得新增其他色碼，不得使用 `rgb()` / `hsl()`
- **最多 2 種字體**：Barlow Condensed（`var(--display)`）／ Inter（`var(--body)`）。不加 `@font-face`
- **零建置、零相依**：不得新增 `package.json`、`node_modules` 或任何 npm 套件
- **不得出現任何個人姓名或個人聯絡方式**；只能用組織信箱 `beyondtaiwan2020@gmail.com`
- **既有的中文文案一個字都不要改寫或潤飾**
- **這一輪不改任何文案、不動資料庫、不碰 `activities` 的內容**（spec §9）
- **不改 `render()` 的重繪方式**（spec §5.1）
- 註解要寫理由。放進 template literal 的註解會變成輸出的一部分——這個 repo 被咬過三次，寫之前先想它會不會撞到測試斷言

---

## File Structure

| 檔案 | 動作 | 責任 |
|---|---|---|
| `src/ui.js` | 修改 | 新增 `faceOf()`、`flipBtnHTML()`；改寫 `slotHTML()` |
| `src/main.js` | 修改 | `S.flipped` / `S.justFlipped` 初始值；`flip` 事件 |
| `index.html` | 修改 | `.flip` / `.face` / `.faceopen` / `.flipbtn` 樣式、翻面 keyframes、reduce 區塊 |
| `check-motion.mjs` | 新增 | 掃 CSS 的動畫涵蓋率，由 `check.sh` 呼叫 |
| `check.sh` | 修改 | 呼叫 `check-motion.mjs` |
| `test/ui-slot.test.mjs` | 修改 | 翻面的結構與狀態測試 |

---

## Task 1：`faceOf` 純函式

**Files:**
- Modify: `src/ui.js`（`milestoneState` 之後）
- Test: `test/ui-slot.test.mjs`

**Interfaces:**
- Consumes: 無
- Produces: `export function faceOf(S, a) -> "front" | "back"`

- [ ] **Step 1: 寫失敗的測試**

加進 `test/ui-slot.test.mjs`（該檔案已有 `act` 與 `state` 工廠，擴充它們而不是複製）：

```js
test("faceOf：未蓋章一律正面", () => {
  assert.equal(faceOf(state(), act), "front");
  // 就算 flipped 說背面也一樣 —— 未蓋章根本沒有背面可以翻到
  assert.equal(faceOf({ ...state(), flipped: { "09A": "back" } }, act), "front");
});

test("faceOf：已蓋章預設背面", () => {
  assert.equal(faceOf(state({ "09A": { date: "2026-09-05" } }), act), "back");
});

test("faceOf：已蓋章時 flipped 可以覆寫成正面", () => {
  const S = { ...state({ "09A": { date: "2026-09-05" } }), flipped: { "09A": "front" } };
  assert.equal(faceOf(S, act), "front");
});

test("faceOf：S.flipped 不存在時不炸", () => {
  // 舊形狀的 state，或還沒有人翻過任何一格
  const S = state({ "09A": { date: "2026-09-05" } });
  delete S.flipped;
  assert.equal(faceOf(S, act), "back");
});
```

- [ ] **Step 2: 跑測試確認它失敗**

Run: `cd /Users/pinwang/bt-passport && node --test test/*.test.mjs`
Expected: FAIL，`does not provide an export named 'faceOf'`

- [ ] **Step 3: 實作**

在 `src/ui.js` 的 `milestoneState` 之後插入：

```js
// 這一格現在哪一面朝上。**只有這一個定義點** —— slotHTML 與任何之後要知道
// 面向的地方都問它，不准任何一邊自己再判斷一次。跟 SLOT_ORDER、pagesOf、
// milestoneState 同一條原則：兩個地方各判斷一次，改了其中一邊另一邊會靜靜地說謊。
//
// 未蓋章一律回 "front"，而且**不看 S.flipped** —— 未蓋章的格子根本不產生背面的 DOM
// （spec §3.1），所以「翻到背面」是一個不存在的狀態，不該讓它表達得出來。
//
// S.flipped 只活在 session 裡：不進資料庫、不進備份檔、不進 localStorage。
// 它是介面狀態不是護照內容（spec §4）。
export function faceOf(S, a) {
  if (!S.stamps[a.id]) return "front";
  return (S.flipped || {})[a.id] === "front" ? "front" : "back";
}
```

- [ ] **Step 4: 跑測試確認全過**

Run: `cd /Users/pinwang/bt-passport && node --test test/*.test.mjs`
Expected: PASS，比原本多 4 條

- [ ] **Step 5: Commit**

```bash
git add src/ui.js test/ui-slot.test.mjs
git commit -m "feat(ui): faceOf —— 格子面向的唯一定義點

未蓋章一律正面且不看 S.flipped：未蓋章的格子不產生背面的 DOM，
所以「翻到背面」是不存在的狀態，不該讓它表達得出來。"
```

---

## Task 2：結構、樣式、事件

這個 Task 做完，翻面就能用了，只是還沒有動畫。

**Files:**
- Modify: `src/ui.js`（`slotHTML`、新增 `flipBtnHTML`）
- Modify: `src/main.js`（state 初值、`flip` 事件）
- Modify: `index.html`（新樣式）
- Test: `test/ui-slot.test.mjs`

**Interfaces:**
- Consumes: Task 1 的 `faceOf(S, a)`
- Produces: `slotHTML` 的輸出結構（`.slot > .flip > .face.front + .face.back`）；`S.flipped`、`S.justFlipped`

- [ ] **Step 1: 寫失敗的測試**

```js
test("未蓋章：只有正面，沒有背面也沒有翻面按鈕", () => {
  const html = slotHTML(state(), act);
  assert.ok(html.includes('class="face front"'));
  assert.ok(!html.includes('class="face back"'), "未蓋章不該產生背面的 DOM");
  assert.ok(!html.includes('data-act="flip"'), "沒有東西可以翻到就不該有翻面按鈕");
  assert.match(html, /data-face="front"/);
});

test("已蓋章：兩面都在，預設背面朝上，兩面各有翻面按鈕", () => {
  const html = slotHTML(state({ "09A": { date: "2026-09-05" } }), act);
  assert.ok(html.includes('class="face front"'));
  assert.ok(html.includes('class="face back"'));
  assert.match(html, /data-face="back"/);
  assert.equal((html.match(/data-act="flip"/g) || []).length, 2, "兩面各一顆");
});

test("朝外的那一面標 aria-hidden，讀螢幕的人不該聽到兩份內容", () => {
  const html = slotHTML(state({ "09A": { date: "2026-09-05" } }), act);
  assert.match(html, /class="face front" aria-hidden="true"/);
  assert.match(html, /class="face back" aria-hidden="false"/);
});

test("翻面按鈕的 aria-label 說得出它要去哪一面", () => {
  const html = slotHTML(state({ "09A": { date: "2026-09-05" } }), act);
  assert.ok(html.includes('aria-label="翻到正面"'));
  assert.ok(html.includes('aria-label="翻到背面"'));
});

test("翻面按鈕不是 faceopen 的子孫 —— 按鈕不能巢狀", () => {
  // 巢狀按鈕是無效的 HTML，而且會讓 closest("[data-act]") 的委派失效。
  const html = slotHTML(state({ "09A": { date: "2026-09-05" } }), act);
  const open = html.indexOf('data-act="open"');
  const closeOpen = html.indexOf("</button>", open);
  const firstFlip = html.indexOf('data-act="flip"');
  assert.ok(firstFlip > closeOpen, "翻面按鈕要在 faceopen 關閉之後");
});

test("一格仍然只有一個 .slot —— 既有的 slotCount 不能被拆成兩個", () => {
  const html = slotHTML(state({ "09A": { date: "2026-09-05" } }), act);
  assert.equal(html.split('class="slot"').length - 1, 1);
});
```

- [ ] **Step 2: 跑測試確認它失敗**

Run: `cd /Users/pinwang/bt-passport && node --test test/*.test.mjs`
Expected: FAIL，多條（現在的 `slotHTML` 沒有 `.face`、沒有 `data-face`）

- [ ] **Step 3: 改寫 `slotHTML`**

把 `src/ui.js` 現有的整個 `slotHTML` 換成：

```js
// 翻面按鈕。**必須是 .faceopen 的兄弟不是子孫** —— 按鈕不能巢狀（無效的 HTML），
// 而且既有的事件委派是 e.target.closest("[data-act]")：放在外面的話，
// 點按鈕會先找到 flip、點面上其他地方會找到 open，天生就對，
// 不需要 stopPropagation。放進去的話兩者都會變成 open。
//
// 44px 的最小點擊區是 spec §3.3 的要求：手機一格佔滿寬度，滑動時很容易誤觸，
// 所以翻面只能由這顆觸發，不能「點整格就翻」。
function flipBtnHTML(id, to) {
  return `<button class="flipbtn" data-act="flip" data-id="${id}" data-to="${to}" aria-label="${to === "back" ? "翻到背面" : "翻到正面"}">↻</button>`;
}

// CATNAME 取不到就印空字串。取不到代表有人改了 category 名稱，
// 那時候該壞的是順序（排到最後），不是在畫面上出現「undefined」四個字。
//
// 這段刻意是 JS 註解而不是 template literal 裡的 HTML 註解：
// 放進 template 的話它會變成輸出的一部分，而 test/ui-order.test.mjs 的
// mutation 測試斷言的正是「輸出裡不准出現 undefined 這個字」——
// 註解自己會把那個測試弄紅。另外 slotHTML 每次整頁渲染會被呼叫 33 次，
// HTML 註解等於同一段文字送 33 份到瀏覽器。
//
// 未蓋章**不產生背面的 DOM**（spec §3.1）：沒有東西可以翻到時不該有翻面按鈕，
// 而一個空的背面只會被讀螢幕的人聽到、被下一個人誤以為是 bug。
export function slotHTML(S, a) {
  const st = S.stamps[a.id];
  const entry = S.entries[a.id] || {};
  const face = faceOf(S, a);
  const anim = st && S.justStamped === a.id;
  if (anim) S.justStamped = null;

  const front = `<div class="face front" aria-hidden="${face === "back"}">
      <button class="faceopen" data-act="open" data-id="${a.id}">
        <span class="cat">${esc(CATNAME[a.category] || "")}</span>
        <span class="ttl">${esc(a.title_zh)}</span>
        <span class="en">${esc(a.title_en)}</span>
        <span class="hint">${esc(a.description)}</span>
        ${st ? "" : `<span class="cta">蓋章 →</span>`}
      </button>
      ${st ? flipBtnHTML(a.id, "back") : ""}
    </div>`;

  const back = !st ? "" : `<div class="face back" aria-hidden="${face === "front"}">
      ${stampHTML(a, st, anim)}
      ${entry.note ? `<span class="note">${esc(entry.note)}</span>` : ""}
      ${entry.photo ? `<img class="thumb" src="${esc(entry.photo)}" alt="">` : ""}
      ${flipBtnHTML(a.id, "front")}
    </div>`;

  return `<div class="slot" data-id="${a.id}" data-done="${st ? 1 : 0}" data-face="${face}">
    <div class="flip">${front}${back}</div>
  </div>`;
}
```

- [ ] **Step 4: `src/main.js` 加狀態與事件**

初始 state（第 11-17 行那個物件）加兩個 key：

```js
  down: false, justStamped: null, flipped: {}, justFlipped: null
```

「清除這本護照」重建 state 的地方（`act === "reset"` 裡）也要加同樣兩個 key，
否則清除之後翻面會炸。

在 `if (act === "open")` 那一行**之前**插入：

```js
  // 翻面。只改介面狀態，不碰資料庫 —— 哪一面朝上不是護照內容（spec §4）。
  if (act === "flip") {
    const id = b.dataset.id;
    S.flipped[id] = b.dataset.to;
    S.justFlipped = id;
    render();
    return;
  }
```

- [ ] **Step 5: `index.html` 加樣式**

在 `.slot[data-done="1"]` 那一行**之後**插入：

```css
  /* ── 翻面卡（spec 2026-08-25）──
     這是新的視覺元件，是原規格 §3.4 的**明確例外**，使用者 2026-08-25 授權。
     理由：一格要塞五樣東西，蓋完章之後說明變成死重量，而護照本來就是一頁一頁翻的。
     這個例外只涵蓋翻面卡本身，不是「以後可以自由新增元件」的先例。

     .flip 用 grid 把兩面疊在同一格，**容器高度自動是兩面的最大值** ——
     不用 JS 量、不用寫死數字，內容改了也不用回來調。
     2026-08-25 實測：只放正面 88px、只放背面 145px、兩面疊放 145px、翻面前後不跳。

     backface-visibility:hidden 同時擋掉指標事件（實測），所以背面朝外時
     點中央命中的是背面的內容，不需要額外的 pointer-events 補丁。 */
  .slot{perspective:900px}
  .slot .flip{display:grid;flex:1;transform-style:preserve-3d}
  .slot .face{grid-area:1/1;backface-visibility:hidden;display:flex;flex-direction:column}
  .slot .face.back{transform:rotateY(180deg)}
  .slot[data-face="back"] .flip{transform:rotateY(180deg)}
  .slot .faceopen{display:flex;flex-direction:column;text-align:left;width:100%;flex:1}
  /* 翻面按鈕走一般流排在最後、靠右，不用絕對定位 ——
     絕對定位會蓋到照片或心得，而且不計入高度；這樣排它永遠不會重疊。
     44px 是 spec §3.3 的最小點擊區要求（手機誤觸）。 */
  .slot .flipbtn{
    align-self:flex-end;margin-top:auto;min-width:44px;min-height:44px;
    display:flex;align-items:center;justify-content:center;
    font-size:16px;opacity:.4;border-radius:2px;
  }
  .slot .flipbtn:hover,.slot .flipbtn:focus-visible{opacity:.9}
```

- [ ] **Step 6: 跑測試確認全過**

Run: `cd /Users/pinwang/bt-passport && node --test test/*.test.mjs`
Expected: PASS。既有的 `蓋章 →`、`stampwrap`、`slotCount`、`undefined` 那幾條都要還是綠的——
**任何一條變紅就停下來回報**，那代表新結構破壞了既有契約。

- [ ] **Step 7: 版面實測（要量，四項）**

用 node 產生預覽頁（讀真正的 `index.html` `<style>`，**一定要加
`<meta name="viewport" content="width=device-width, initial-scale=1">`**），
用 chrome-devtools 的 `emulate` 切寬度（`resize_page` 縮不到 390）。
**每組數字都要附 `window.innerWidth` 與 `devicePixelRatio` 自證。**

一列三格：未蓋章、已蓋章無心得、已蓋章有心得且有照片。1280px 與 390px 各量一次：

1. **翻面前後容器高度不變**（把某一格的 `data-face` 用 JS 切換，量前後）
2. **同列三格等高**
3. **背面朝外時點中央，命中的不是正面**（`document.elementFromPoint`）
4. **翻面按鈕的實際尺寸 ≥ 44×44**

**任何一項不符就停下來回報，不要自己調 CSS 去湊。**

- [ ] **Step 8: Commit**

```bash
git add src/ui.js src/main.js index.html test/ui-slot.test.mjs
git commit -m "feat(ui): 活動格子改成可翻面的卡片（結構與事件，尚無動畫）

.slot 從 button 改成 div —— 按鈕不能巢狀，而翻面按鈕必須在格子裡。
翻面按鈕是 faceopen 的兄弟不是子孫：既有的 closest([data-act]) 委派
天生就會先找到它，不需要 stopPropagation。

兩面用 grid 疊在同一格，容器高度自動是兩面的最大值（實測 88/145/145），
翻面時不跳、同列三格等高。未蓋章不產生背面的 DOM。"
```

---

## Task 3：動畫與時序

**Files:**
- Modify: `src/ui.js`（`slotHTML` 消耗 `justFlipped`）
- Modify: `index.html`（keyframes、reduce 區塊）
- Test: `test/ui-slot.test.mjs`

**Interfaces:**
- Consumes: Task 2 的結構與 `S.justFlipped`
- Produces: `.flip.turning-back` / `.flip.turning-front` 兩個動畫 class

- [ ] **Step 1: 寫失敗的測試**

```js
test("剛翻過的那一格帶動畫 class，而且旗標被消耗掉", () => {
  const S = { ...state({ "09A": { date: "2026-09-05" } }), justFlipped: "09A" };
  const html = slotHTML(S, act);
  assert.match(html, /class="flip turning-back"/);
  assert.equal(S.justFlipped, null, "旗標要被消耗，否則每次重繪都會再播一次");
});

test("沒有剛翻過的格子不帶動畫 class", () => {
  const html = slotHTML(state({ "09A": { date: "2026-09-05" } }), act);
  assert.ok(!html.includes("turning-"));
});

test("翻回正面時用的是另一個方向的動畫", () => {
  const S = { ...state({ "09A": { date: "2026-09-05" } }), flipped: { "09A": "front" }, justFlipped: "09A" };
  assert.match(slotHTML(S, act), /class="flip turning-front"/);
});
```

- [ ] **Step 2: 跑測試確認它失敗**

Run: `cd /Users/pinwang/bt-passport && node --test test/*.test.mjs`
Expected: FAIL，輸出裡沒有 `turning-`

- [ ] **Step 3: `slotHTML` 消耗 `justFlipped`**

在 `const anim = ...; if (anim) S.justStamped = null;` 之後加：

```js
  // 跟 justStamped 同一個模式：讀到就消耗掉，否則每次重繪都會再播一次動畫。
  const turn = S.justFlipped === a.id;
  if (turn) S.justFlipped = null;
```

把最外層的 `<div class="flip">` 改成：

```js
    <div class="flip${turn ? (face === "back" ? " turning-back" : " turning-front") : ""}">${front}${back}</div>
```

- [ ] **Step 4: `index.html` 加 keyframes 與延遲**

在 `@keyframes land` 那組之後插入：

```css
  /* 翻面用 animation 不用 transition：main.js 的 render() 是 el.innerHTML = ...，
     整棵子樹每次都被拆掉重建，transition 永遠不會跑（要過渡的元素沒活到下一幀）。
     既有的 .stamp.land 與 .page.turn 會動也正是這個原因。
     **不要「改善」成 transition，也不要為了它去改 render() 的重繪方式**
     —— 那是拿一個穩定的架構去換一個動畫（spec §5.1）。 */
  @keyframes flipToBack{from{transform:rotateY(0)}to{transform:rotateY(180deg)}}
  @keyframes flipToFront{from{transform:rotateY(180deg)}to{transform:rotateY(0)}}
  .flip.turning-back{animation:flipToBack .42s ease}
  .flip.turning-front{animation:flipToFront .42s ease}
  /* 蓋章之後：先翻面、翻完再落章。用 animation-delay 串接**不要用 JS 計時器** ——
     計時器會跟下一次 render() 競態（使用者在動畫沒跑完時翻頁或蓋下一個章，
     計時器仍然會觸發，而它要操作的 DOM 已經不在了）。animation-delay 沒有這個問題：
     元素被移除，動畫跟著消失。
     animation-fill-mode:backwards 不可省：land 的 0% 是 opacity 0，
     少了它的話章會在延遲的那 .42 秒裡先以最終樣子出現，然後才彈一次。 */
  .flip.turning-back .stamp.land{animation-delay:.42s;animation-fill-mode:backwards}
```

- [ ] **Step 5: 加進 reduce 區塊**

把既有那一行改成：

```css
    .stamp.land,.page.turn,.overprint.land,.flip.turning-back,.flip.turning-front{animation:none}
```

**註解那段講「三個動畫」的字要一併改成正確的數字。** 錯的理由比沒有更糟
（見 README 的「這些決定是刻意的」）。

- [ ] **Step 6: 跑測試確認全過**

Run: `cd /Users/pinwang/bt-passport && node --test test/*.test.mjs`
Expected: PASS

- [ ] **Step 7: 時序實測**

在瀏覽器裡驗兩件事，附 `window.innerWidth` 自證：

1. **翻面動畫真的會播**：渲染一個帶 `turning-back` 的格子，
   用 `getAnimations()` 讀 `.flip` 上的動畫，確認名稱是 `flipToBack`、`duration` 是 420ms
2. **章的延遲真的等於翻面時長**：讀 `.stamp.land` 的 `animationDelay` 與 `animationFillMode`，
   確認是 `0.42s` 與 `backwards`

再驗 reduced-motion：用 `emulate` 的 `colorScheme` 沒有 reduced-motion 選項，
所以改用 `Emulation.setEmulatedMedia` 或直接在預覽頁加一段
`@media(prefers-reduced-motion:reduce)` 的等效測試——**做得到就做，做不到就在報告寫明
並改用讀 CSS 規則的方式確認那五個選擇器都在 reduce 區塊裡**。

- [ ] **Step 8: Commit**

```bash
git add src/ui.js index.html test/ui-slot.test.mjs
git commit -m "feat(ui): 翻面動畫，蓋章後先翻面再落章

兩段用 animation-delay 串接不用 JS 計時器：計時器會跟下一次 render() 競態，
元素已經不在了它還是會觸發。animation-fill-mode:backwards 不可省，
少了它章會在延遲期間先以最終樣子出現。

翻面的兩個 keyframe 一併加進 prefers-reduced-motion 的關閉清單。"
```

---

## Task 4：reduced-motion 的通用檢查

**Files:**
- Create: `check-motion.mjs`
- Modify: `check.sh`

**Interfaces:**
- Consumes: 無
- Produces: `check.sh` 的第 23 項檢查

- [ ] **Step 1: 寫檢查腳本**

Create `check-motion.mjs`：

```js
// prefers-reduced-motion 的涵蓋率檢查。由 check.sh 呼叫。
//
// 為什麼要有這支：2026-08-18 做 §11-17 驗收時發現 .overprint.land 漏在
// reduce 區塊之外——它只在集滿 33 格那一刻出現，平常根本測不到，
// 是人工用 CSSOM 逐條比對才抓到的。這支讓那件事不可能再發生。
//
// **這是無障礙需求不是視覺偏好**（使用者 2026-08-25 的裁定），所以由機器守門。
//
// 判準兩種：
//   animation  —— 一律要在 reduce 區塊裡被關掉
//   transition —— 只有動到 transform 或位置類屬性時才要求；
//                 顏色與透明度的過渡不是前庭刺激來源
import fs from "node:fs";

const html = fs.readFileSync("index.html", "utf8");
const css = html.match(/<style>([\s\S]*?)<\/style>/)[1]
  .replace(/\/\*[\s\S]*?\*\//g, "");   // 先剝註解，否則註解裡的文字會被當成選擇器

const rules = block => [...block.matchAll(/([^{}]+)\{([^}]*)\}/g)];
const selectorsOf = sel => sel.split(",").map(s => s.trim()).filter(Boolean);

const declared = new Set(), transitions = [];
for (const [, sel, body] of rules(css)) {
  const s = sel.trim();
  if (s.startsWith("@") || s.includes("%") || s === "from" || s === "to") continue;
  const a = body.match(/(?<![-\w])animation\s*:\s*([^;]+)/);
  if (a && a[1].trim() !== "none") selectorsOf(s).forEach(x => declared.add(x));
  const t = body.match(/(?<![-\w])transition\s*:\s*([^;]+)/);
  if (t) transitions.push([s, t[1].trim()]);
}

const rm = css.match(/@media\s*\(prefers-reduced-motion\s*:\s*reduce\)\s*\{([\s\S]*?)\n\s{0,4}\}/);
const disabled = new Set();
for (const [, sel, body] of rules(rm ? rm[1] : "")) {
  if (/animation\s*:\s*none/.test(body)) selectorsOf(sel).forEach(x => disabled.add(x));
}

const MOTION = ["transform", "top", "left", "right", "bottom", "width", "height", "inset", "margin"];
const missing = [...declared].filter(s => !disabled.has(s));
const risky = transitions.filter(([, v]) =>
  v.split(",").map(p => p.trim().split(/\s/)[0]).some(p => MOTION.some(k => p.startsWith(k))));

if (missing.length === 0 && risky.length === 0) {
  console.log(`animation ${declared.size} 個全部被 reduce 關掉；transition ${transitions.length} 個都沒動到位置或形狀`);
  process.exit(0);
}
for (const s of missing) console.log(`  漏掉的 animation：${s}`);
for (const [s, v] of risky) console.log(`  會動位置/形狀的 transition：${s} { ${v} }`);
process.exit(1);
```

- [ ] **Step 2: 直接跑一次，確認現況是綠的**

Run: `cd /Users/pinwang/bt-passport && node check-motion.mjs; echo "exit=$?"`
Expected: 印出 `animation 5 個全部被 reduce 關掉；transition 3 個都沒動到位置或形狀`，`exit=0`

（5 個是 `.page.turn`、`.stamp.land`、`.overprint.land`、`.flip.turning-back`、`.flip.turning-front`。
數字不同就以實際為準並在報告說明。）

- [ ] **Step 3: 破壞一次，確認會抓到**

暫時把 reduce 那一行的 `.flip.turning-back` 刪掉，重跑：

Run: `node check-motion.mjs; echo "exit=$?"`
Expected: 印出 `漏掉的 animation：.flip.turning-back`，`exit=1`

**還原用 `cp`（先備份），不要用 `git checkout`** —— 工作目錄有未提交的東西時
`git checkout <file>` 會把它一起丟掉。還原後 `git status --short` 確認乾淨。

- [ ] **Step 4: 再破壞一次，確認 transition 那半也會抓到**

暫時把 `.slot` 的 `transition:border-color .16s,background .16s` 改成
`transition:transform .16s`，重跑：

Expected: 印出 `會動位置/形狀的 transition：.slot { transform .16s }`，`exit=1`

一樣用 `cp` 還原並確認乾淨。

- [ ] **Step 5: 掛進 `check.sh`**

在 `# CNAME 不可掉` 那一段**之前**插入：

```bash
# prefers-reduced-motion 的涵蓋率。**這是無障礙需求不是視覺偏好**，所以由機器守門，
# 不只寫在 CSS 裡（使用者 2026-08-25 的裁定）。
# 2026-08-18 那次 .overprint.land 漏在 reduce 之外，是人工逐條比對才抓到的 ——
# 它只在集滿 33 格那一刻出現，平常測不到。這支讓那件事不可能再發生。
if command -v node >/dev/null 2>&1; then
  motion=$(node check-motion.mjs 2>&1)
  if [ $? -eq 0 ]; then
    ok "reduced-motion 涵蓋所有動畫（$motion）"
  else
    bad "有動畫沒有被 prefers-reduced-motion 關掉："
    printf '%s\n' "$motion"
  fi
else
  bad "找不到 node，reduced-motion 檢查沒有跑到（這不是通過）"
fi
```

- [ ] **Step 6: 跑 `check.sh`**

Run: `cd /Users/pinwang/bt-passport && ./check.sh`
Expected: 全部 `ok`，含新增那一項，最後一行 `全部通過。`

- [ ] **Step 7: Commit**

```bash
git add check-motion.mjs check.sh
git commit -m "test(check): reduced-motion 的通用涵蓋率檢查

不是只釘翻面那一條，是掃出所有宣告 animation 的選擇器跟 reduce 區塊比對。
2026-08-18 的 .overprint.land 就是漏網的那種——只在集滿那一刻出現、平常測不到，
當時是人工用 CSSOM 逐條比對才抓到。

transition 一起掃但判準不同：只有動到 transform 或位置類屬性才要求關閉，
顏色與透明度的過渡不是前庭刺激來源。"
```

---

## Self-Review

**規格覆蓋率**：§2（§3.4 例外）→ Task 2 Step 5 的 CSS 註解；§3 結構 → Task 2；§3.1 未蓋章不產生背面 → Task 1 的 `faceOf` + Task 2 的測試；§3.2 點擊分派 → Task 2 Step 1 的巢狀測試；§3.3 手機 44px → Task 2 Step 5 的 CSS 與 Step 7 的實測；§4 狀態 → Task 1 + Task 2 Step 4；§5.1 不用 transition → Task 3 Step 4 的註解；§5.2 animation-delay → Task 3 Step 4；§5.3 通用檢查 → Task 4；§6 高度 → Task 2 Step 5 的 CSS 與 Step 7 的實測；§6.1 背面不吃點擊 → Task 2 Step 7 第 3 項；§7 無障礙 → Task 2 的 aria 測試與 44px；§8 測試 → 各 Task；§9 不做的事 → 沒有任何 Task 碰到那五項。

**型別一致性**：`faceOf(S, a)` 在 Task 1 定義、Task 2 消費，簽名一致；`flipBtnHTML(id, to)` 只在 Task 2 出現；`turning-back` / `turning-front` 在 Task 3 定義並在同一個 Task 的 CSS 使用；`S.flipped` / `S.justFlipped` 在 Task 2 Step 4 初始化、Task 2 與 Task 3 消費。

**已知的順序相依**：Task 2 依賴 Task 1 的 `faceOf`；Task 3 依賴 Task 2 的結構；Task 4 的預期數字（5 個 animation）依賴 Task 3 已經加入兩個翻面動畫。
